// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.4
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xber_stats.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XBer_stats_CfgInitialize(XBer_stats *InstancePtr, XBer_stats_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Cntrl_BaseAddress = ConfigPtr->Cntrl_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

void XBer_stats_Start(XBer_stats *InstancePtr) {
    u32 Data;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_AP_CTRL) & 0x80;
    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_AP_CTRL, Data | 0x01);
}

u32 XBer_stats_IsDone(XBer_stats *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_AP_CTRL);
    return (Data >> 1) & 0x1;
}

u32 XBer_stats_IsIdle(XBer_stats *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_AP_CTRL);
    return (Data >> 2) & 0x1;
}

u32 XBer_stats_IsReady(XBer_stats *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_AP_CTRL);
    // check ap_start to see if the pcore is ready for next input
    return !(Data & 0x1);
}

void XBer_stats_EnableAutoRestart(XBer_stats *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_AP_CTRL, 0x80);
}

void XBer_stats_DisableAutoRestart(XBer_stats *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_AP_CTRL, 0);
}

void XBer_stats_Set_num_defs(XBer_stats *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_NUM_DEFS_DATA, Data);
}

u32 XBer_stats_Get_num_defs(XBer_stats *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_NUM_DEFS_DATA);
    return Data;
}

void XBer_stats_Set_num_blocks(XBer_stats *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_NUM_BLOCKS_DATA, Data);
}

u32 XBer_stats_Get_num_blocks(XBer_stats *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_NUM_BLOCKS_DATA);
    return Data;
}

u32 XBer_stats_Get_k_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_K_BASE);
}

u32 XBer_stats_Get_k_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_K_HIGH);
}

u32 XBer_stats_Get_k_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_K_HIGH - XBER_STATS_CNTRL_ADDR_K_BASE + 1);
}

u32 XBer_stats_Get_k_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_K;
}

u32 XBer_stats_Get_k_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_K;
}

u32 XBer_stats_Write_k_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_K_HIGH - XBER_STATS_CNTRL_ADDR_K_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_K_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_k_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_K_HIGH - XBER_STATS_CNTRL_ADDR_K_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_K_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_k_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_K_HIGH - XBER_STATS_CNTRL_ADDR_K_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_K_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_k_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_K_HIGH - XBER_STATS_CNTRL_ADDR_K_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_K_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_n_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_N_BASE);
}

u32 XBer_stats_Get_n_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_N_HIGH);
}

u32 XBer_stats_Get_n_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_N_HIGH - XBER_STATS_CNTRL_ADDR_N_BASE + 1);
}

u32 XBer_stats_Get_n_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_N;
}

u32 XBer_stats_Get_n_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_N;
}

u32 XBer_stats_Write_n_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_N_HIGH - XBER_STATS_CNTRL_ADDR_N_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_N_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_n_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_N_HIGH - XBER_STATS_CNTRL_ADDR_N_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_N_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_n_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_N_HIGH - XBER_STATS_CNTRL_ADDR_N_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_N_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_n_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_N_HIGH - XBER_STATS_CNTRL_ADDR_N_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_N_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_mask_V_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_MASK_V_BASE);
}

u32 XBer_stats_Get_mask_V_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_MASK_V_HIGH);
}

u32 XBer_stats_Get_mask_V_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_MASK_V_HIGH - XBER_STATS_CNTRL_ADDR_MASK_V_BASE + 1);
}

u32 XBer_stats_Get_mask_V_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_MASK_V;
}

u32 XBer_stats_Get_mask_V_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_MASK_V;
}

u32 XBer_stats_Write_mask_V_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_MASK_V_HIGH - XBER_STATS_CNTRL_ADDR_MASK_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_MASK_V_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_mask_V_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_MASK_V_HIGH - XBER_STATS_CNTRL_ADDR_MASK_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_MASK_V_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_mask_V_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_MASK_V_HIGH - XBER_STATS_CNTRL_ADDR_MASK_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_MASK_V_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_mask_V_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_MASK_V_HIGH - XBER_STATS_CNTRL_ADDR_MASK_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_MASK_V_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_block_def_cfg_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE);
}

u32 XBer_stats_Get_block_def_cfg_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_HIGH);
}

u32 XBer_stats_Get_block_def_cfg_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + 1);
}

u32 XBer_stats_Get_block_def_cfg_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_BLOCK_DEF_CFG;
}

u32 XBer_stats_Get_block_def_cfg_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_BLOCK_DEF_CFG;
}

u32 XBer_stats_Write_block_def_cfg_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_block_def_cfg_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_block_def_cfg_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_block_def_cfg_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CFG_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_block_def_cnt_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE);
}

u32 XBer_stats_Get_block_def_cnt_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_HIGH);
}

u32 XBer_stats_Get_block_def_cnt_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + 1);
}

u32 XBer_stats_Get_block_def_cnt_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_BLOCK_DEF_CNT;
}

u32 XBer_stats_Get_block_def_cnt_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_BLOCK_DEF_CNT;
}

u32 XBer_stats_Write_block_def_cnt_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_block_def_cnt_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_block_def_cnt_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_block_def_cnt_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BLOCK_DEF_CNT_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_ber_stats_raw_berr_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE);
}

u32 XBer_stats_Get_ber_stats_raw_berr_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_HIGH);
}

u32 XBer_stats_Get_ber_stats_raw_berr_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + 1);
}

u32 XBer_stats_Get_ber_stats_raw_berr_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_BER_STATS_RAW_BERR;
}

u32 XBer_stats_Get_ber_stats_raw_berr_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_BER_STATS_RAW_BERR;
}

u32 XBer_stats_Write_ber_stats_raw_berr_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_raw_berr_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_ber_stats_raw_berr_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_raw_berr_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_RAW_BERR_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_ber_stats_cor_berr_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE);
}

u32 XBer_stats_Get_ber_stats_cor_berr_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_HIGH);
}

u32 XBer_stats_Get_ber_stats_cor_berr_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + 1);
}

u32 XBer_stats_Get_ber_stats_cor_berr_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_BER_STATS_COR_BERR;
}

u32 XBer_stats_Get_ber_stats_cor_berr_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_BER_STATS_COR_BERR;
}

u32 XBer_stats_Write_ber_stats_cor_berr_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_cor_berr_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_ber_stats_cor_berr_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_cor_berr_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BERR_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_ber_stats_cor_blerr_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE);
}

u32 XBer_stats_Get_ber_stats_cor_blerr_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_HIGH);
}

u32 XBer_stats_Get_ber_stats_cor_blerr_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + 1);
}

u32 XBer_stats_Get_ber_stats_cor_blerr_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_BER_STATS_COR_BLERR;
}

u32 XBer_stats_Get_ber_stats_cor_blerr_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_BER_STATS_COR_BLERR;
}

u32 XBer_stats_Write_ber_stats_cor_blerr_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_cor_blerr_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_ber_stats_cor_blerr_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_cor_blerr_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_COR_BLERR_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_ber_stats_iter_cnt_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE);
}

u32 XBer_stats_Get_ber_stats_iter_cnt_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_HIGH);
}

u32 XBer_stats_Get_ber_stats_iter_cnt_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + 1);
}

u32 XBer_stats_Get_ber_stats_iter_cnt_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_BER_STATS_ITER_CNT;
}

u32 XBer_stats_Get_ber_stats_iter_cnt_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_BER_STATS_ITER_CNT;
}

u32 XBer_stats_Write_ber_stats_iter_cnt_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_iter_cnt_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_ber_stats_iter_cnt_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_iter_cnt_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_ITER_CNT_BASE + offset + i);
    }
    return length;
}

u32 XBer_stats_Get_ber_stats_block_cnt_BaseAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE);
}

u32 XBer_stats_Get_ber_stats_block_cnt_HighAddress(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_HIGH);
}

u32 XBer_stats_Get_ber_stats_block_cnt_TotalBytes(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + 1);
}

u32 XBer_stats_Get_ber_stats_block_cnt_BitWidth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_WIDTH_BER_STATS_BLOCK_CNT;
}

u32 XBer_stats_Get_ber_stats_block_cnt_Depth(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBER_STATS_CNTRL_DEPTH_BER_STATS_BLOCK_CNT;
}

u32 XBer_stats_Write_ber_stats_block_cnt_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_block_cnt_Words(XBer_stats *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + (offset + i)*4);
    }
    return length;
}

u32 XBer_stats_Write_ber_stats_block_cnt_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XBer_stats_Read_ber_stats_block_cnt_Bytes(XBer_stats *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_HIGH - XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Cntrl_BaseAddress + XBER_STATS_CNTRL_ADDR_BER_STATS_BLOCK_CNT_BASE + offset + i);
    }
    return length;
}

void XBer_stats_InterruptGlobalEnable(XBer_stats *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_GIE, 1);
}

void XBer_stats_InterruptGlobalDisable(XBer_stats *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_GIE, 0);
}

void XBer_stats_InterruptEnable(XBer_stats *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_IER);
    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_IER, Register | Mask);
}

void XBer_stats_InterruptDisable(XBer_stats *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_IER);
    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_IER, Register & (~Mask));
}

void XBer_stats_InterruptClear(XBer_stats *InstancePtr, u32 Mask) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XBer_stats_WriteReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_ISR, Mask);
}

u32 XBer_stats_InterruptGetEnabled(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_IER);
}

u32 XBer_stats_InterruptGetStatus(XBer_stats *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XBer_stats_ReadReg(InstancePtr->Cntrl_BaseAddress, XBER_STATS_CNTRL_ADDR_ISR);
}

